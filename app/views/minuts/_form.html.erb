<%= form_for(@minut) do |f| %>
  <% if @minut.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@minut.errors.count, "error") %> prohibited this minut from being saved:</h2>

      <ul>
      <% @minut.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "タイトル：" %><br>
    <%= f.text_area :title, :size=>"40x1" %>
  </div>
  <div class="field">
    <%= f.label "本文：" %><br>
    <%= f.text_area :body, :size=>"40x10" %>
  </div>
  <div class="field">
     <%= f.label "参照権限" %>
     <br>
     <% @users.each do |user| %>
      <%= f.check_box :id, {multiple: true}, user.id, nil %>
      <%= f.label :user_id, user.user_name, :value => user.id %>
      <br>
     <% end %>   
  </div>

  <%= f.fields_for :minuti_details do |addresses_form| %>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
